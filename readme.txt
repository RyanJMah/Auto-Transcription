The purpose of this project is to try to make an automatic music transcriber.

In progress.

pydub requires a program called "ffmpeg" to function. To install it, run
   
    >> sudo apt-get install ffmpeg

on ubuntu.